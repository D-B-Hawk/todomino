{
  "build": {
    "dockerfile": "../Dockerfile",
    "context": "..",
    "target": "development"
  },
  "mounts": ["source=${localEnv:HOME}/.ssh,target=/root/.ssh,type=bind"],

  "forwardPorts": [5173]
}
